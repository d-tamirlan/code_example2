version: '3'

services:
  db:
    image: postgres
    env_file: .env

  web:
    restart: always
    build: .
    command: /bin/bash -c "pip install -r requirements.txt; python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/src
    ports:
      - "8000:8000"
    depends_on:
      - db